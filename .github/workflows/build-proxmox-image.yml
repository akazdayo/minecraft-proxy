name: Build NixOS Proxmox Image

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"

concurrency:
  group: proxmox-image-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  flake-update:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      has_changes: ${{ steps.check_changes.outputs.has_changes }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v21

      - name: Enable Magic Nix Cache
        uses: DeterminateSystems/magic-nix-cache-action@v13
        with:
          use-flakehub: false

      - name: Update flake.lock
        run: nix flake update

      - name: Check for changes
        id: check_changes
        run: |
          if git diff --quiet flake.lock; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
            echo "No changes in flake.lock"
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
            echo "Changes detected in flake.lock"
          fi

      - name: Upload updated flake.lock
        if: steps.check_changes.outputs.has_changes == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: flake-lock-proxmox
          path: flake.lock
          if-no-files-found: error
          retention-days: 1

  build:
    runs-on: ubuntu-latest
    needs: flake-update
    if: needs.flake-update.outputs.has_changes == 'true'
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Download updated flake.lock
        uses: actions/download-artifact@v4
        with:
          name: flake-lock-proxmox

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v21

      - name: Enable Magic Nix Cache
        uses: DeterminateSystems/magic-nix-cache-action@v13
        with:
          use-flakehub: false

      - name: Build Proxmox LXC image
        run: nix build -L --option system-features "" ".#proxmox-image"

      - name: Locate image file
        id: out
        run: |
          IMG=$(find -L result -type f -name '*.tar.xz' -print -quit)
          test -f "$IMG"
          echo "image_path=$IMG" >> "$GITHUB_OUTPUT"

      - name: Upload image artifact
        uses: actions/upload-artifact@v4
        with:
          name: nixos-proxmox
          path: ${{ steps.out.outputs.image_path }}
          if-no-files-found: error
          retention-days: 14

  commit:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download updated flake.lock
        uses: actions/download-artifact@v4
        with:
          name: flake-lock-proxmox

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and push changes
        run: |
          git add flake.lock
          git commit -m "chore: update flake.lock (proxmox)" || echo "No changes to commit"
          git push

  release:
    runs-on: ubuntu-latest
    needs: [build, commit]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: nixos-proxmox
          path: dist

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: proxmox-image-${{ github.run_number }}
          files: dist/**
          body: |
            ## NixOS Proxmox LXC Template

            **Run:** ${{ github.run_number }}
            **Commit:** ${{ github.sha }}

            ### テンプレート作成手順
            1. LXCテンプレートをダウンロード
            2. `scp nixos-proxmox-lxc.tar.xz root@<PROXMOX_HOST>:/var/lib/vz/template/cache/`
            3. Proxmox UIまたはCLIからLXCコンテナを作成
          draft: false
          prerelease: false
